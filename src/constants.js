const HOST_PATH = '/home/ritujadixit/Desktop/Clean Coding/clean-test/src/data'

const constants = {
    PATHS: {
        MAPPING_FILE_PATH: `${HOST_PATH}/mapping.json`,
        SAMPLE_DATA_FILE: `${HOST_PATH}/Sample-Data-File.xlsx`,
        SAMPLE_TEMPLATE_FILE: `${HOST_PATH}/Sample-Template-File.xlsx`
    }
}

module.exports = { constants }